---
max_turns: 6
allowed_tools: [Bash, Read, Glob, Grep]
---

A worker named `review-k3x9` was dispatched earlier with agent-tmux on the
`codex` profile. Find out whether it is still running by running
`agent-tmux codex status --json review-k3x9` in the shell, then tell me in one
or two sentences what you did and what you found. Do not invent a status.
