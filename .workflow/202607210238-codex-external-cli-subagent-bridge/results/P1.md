# Result P1

- Implemented one native proxy per authorized external CLI worker, with
  `<cli>_<task>` naming and explicit native/external identity boundaries.
- Added luna-first supervisor selection with terra fallback and sol reserved for
  reasoning-heavy proxy work.
- Added separate headless heartbeat and headed liveness contracts; terminal
  success always requires valid `result.json`.
- Live proxy verdicts: Codex CLI PASS, Claude CLI PASS, agy headed PASS.
- Verification: `scripts/run-all-smokes` exit 0, 60/60 PASS; lint, shellcheck,
  wrapper self-tests, Ruby syntax, and Homebrew style all exit 0.
